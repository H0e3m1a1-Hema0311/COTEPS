import React from 'react';

const Layout: React.FC = ({ children }) => {
    return (
        <div className="layout">
            <header>
                <h1>Welcome to COTEPS</h1>
                {/* Add navigation or logo here */}
            </header>
            <main>
                {children}
            </main>
            <footer>
                <p>&copy; {new Date().getFullYear()} COTEPS. All rights reserved.</p>
            </footer>
        </div>
    );
};

export default Layout;